node_modules/
dist/
# No trailing slash: `.cache/` alone doesn't match a symlink (only a real
# directory), and .cache is a symlink to an external cache location.
.cache
*.log
.DS_Store
.vitest/

web/node_modules/
web/dist/
web/public/data/
